export interface VehicleRecord {
  id: string;
  authUserId: string;
  registrationNumber: string;
  plate: string;
  color: string;
  model: string;
  year: number;
  createdAt: Date;
  updatedAt: Date;
}
