# Terraform (nunca subir providers ni state local)
**/.terraform/
*.tfstate
*.tfstate.*
infra/terraform/environments/*/terraform.tfvars
infra/terraform/bootstrap/terraform.tfvars
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Secrets y llaves (nunca subir .pem ni credenciales del lab)
*.pem
**/*.pem
practicaU*.pem
.env
.env.*
!.env.example

# Manual local con credenciales AWS — usar MANUAL-DESPLIEGUE-EC2.example.md en el repo
MANUAL-DESPLIEGUE-EC2.md
MANUAL-DESPLIEGUE-PROD.md

# OS / IDE
.DS_Store
**/.DS_Store
.Rhistory
*.zip
.idea/
.vscode/

# Node
node_modules/

